export const routes = {
  home: '/',
  chat: '/chat',
  contacts: '/contacts',
  notifications: '/notifications',
  settings: '/settings',
  profile: '/profile',
  auth: {
    login: '/auth/login',
    register: '/register',
  },
} as const;
